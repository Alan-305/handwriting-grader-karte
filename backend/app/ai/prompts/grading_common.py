"""全添削プロンプト共通の採点ルール。"""

ALTERNATIVE_ANSWER_RULE = """
別解の扱い（最重要）:
- 模範解答と異なっていても、文法的に正しく意味が通じる別解を「不可」にしてはならない
- 別解として英文・和文として成立していれば原則「優」。軽微なスペルミス等のみ「良」
- 「不可」は時制・文構造の明白な誤り、意味が通じない、問題の指示に明らかに合致しない場合に限る
- 模範解答との不一致だけを理由に不可としない
"""

USER_PROMPT_ALTERNATIVE_NOTE = (
    "\n\n重要: 模範解答と異なる別解でも、意味・文法が正しければ不可にしないこと。"
)

SCORING_DEDUCTION_RULE = """
採点方法（減点方式）:
- 各問の満点（maxPoints）から、誤りの程度に応じて減点して score を決める
- 優: 満点またはごく軽微な減点（満点の10%以内。例: 10点満点→9〜10点）
- 良: 中程度の減点（満点の20〜40%程度。例: 10点満点→6〜8点）
- 不可: 大幅減点（満点の50%以上。例: 10点満点→0〜5点）
- 複数の誤りがある場合は減点を累積する。score は 0 以上 maxPoints 以下
- maxPoints には指示された配点をそのまま設定すること
- grade（優・良・不可）と score（減点後の得点）は整合させること
"""

USER_PROMPT_SCORING_NOTE = (
    "\n\n採点は満点から誤りの程度に応じて減点すること。"
    "score に減点後の得点、maxPoints にこの問の配点を入れること。"
)

FEEDBACK_EXPLANATION_INSTRUCTION = """
講評（feedback）と解説（explanation）の書き分け（必須）:
- feedback: その設問全体への印象・到達度・次に伸ばす方向を2〜3文でコンパクトにまとめる。文法・語法・表現の個別解説は書かない。
- explanation: 講評と内容が重複しないこと。誤り・改善点・別解の注意などを項目ごとに箇条書きで書く。各項目の先頭は ①、②、③ … の丸数字を使う（最大5項目、各1文程度で簡潔に）。
- feedback に書いた総評を explanation で繰り返さない。

生徒への呼びかけ:
- 「生徒の」「生徒は」など第三者表現は使わない（生徒にそのまま返却する前提）。
- 講評・解説では常に「あなたの」「あなたは」を使う。生徒名は返却プリントの冒頭に記載するため、本文中で名前を呼ばない。
"""

SYMBOL_FEEDBACK_EXPLANATION_INSTRUCTION = """
講評（feedback）と解説（explanation）の書き分け（必須）:
- feedback: 正答数・全体の到達度・次に伸ばす方向を2〜3文でコンパクトにまとめる。個別の正誤解説は書かない。
- explanation: 正誤問題の解説はここだけに書く。模範解答欄は使わず、正答内容も含めて解説の中で完結させる。
  - 小問ごとに改行して1行ずつ書く（箇条書き形式）
  - 各行の先頭は必ず「(1) 正解：」「(2) 不正解：」のように小問番号＋正解/不正解を明記してから解説する
  - 正解: 正答記号・選択内容と、なぜ正しいかを1〜2文で簡潔に
  - 不正解: あなたの解答、正答記号・選択内容、なぜ不正解かを1〜2文で簡潔に
  - 各行の先頭で「正解：」「不正解：」と正誤を明示しているため、文末に「したがって正解。」「よって不正解。」など締めの言い回しは書かない
  - 例:
    (1) 正解：b — 本文の○○から、…
    (2) 不正解：c（正解は a）— a は…
  - 採点対象が1小問のみのときは、指定された小問ラベル（例: (4)）をそのまま使う
  - 1つの答案欄に複数小問が含まれるときは (1)(2)… すべてを漏れなく書く
- feedback に書いた総評を explanation で繰り返さない。

生徒への呼びかけ:
- 「生徒の」「生徒は」など第三者表現は使わない（生徒にそのまま返却する前提）。
- 講評・解説では常に「あなたの」「あなたは」を使う。生徒名は返却プリントの冒頭に記載するため、本文中で名前を呼ばない。
"""

COMPOSITION_FEEDBACK_INSTRUCTION = """
自由英作文の書き分け（必須）:
- feedback（総評）: 全体の出来を2〜3文でコンパクトに評価する。論の展開・文法の個別指摘は書かない
- contentEvaluation（内容について）: あくまで論の展開の仕方、具体例の整合性、読み手に対する説得力の観点から、良い点と改善点を箇条書きで書く。1項目1行、改行で区切る（最大6項目）
- grammarEvaluation（文法・語法・表現について）: 不適切な時制、単語の使い方、表現がある場合のみ指摘する。各項目は必ず「誤り→正しい表現」の順で表記し、その後に修正理由を簡潔に付ける。1項目1行、改行で区切る。該当なしなら「特になし」1行
- polishedAnswer: 上記のアドバイスを盛り込んだ完成版の英文（英語のみ）。問題文の語数指定（例: 60〜80語）は必ず厳守し、範囲外の長文にしてはならない
- 総評と各セクションの内容を重複させない

生徒への呼びかけ:
- 「生徒の」「生徒は」など第三者表現は使わない（生徒にそのまま返却する前提）。
- 講評・解説では常に「あなたの」「あなたは」を使う。生徒名は返却プリントの冒頭に記載するため、本文中で名前を呼ばない。
"""

COMPREHENSIVE_READING_INSTRUCTION = """
長文総合読解（小問単位）の書き分け（必須）:
- feedback（総評）: 当該小問について、全体の出来を2〜3文でコンパクトにまとめる。個別の解説・選択肢の詳説は書かない
- explanation（解説）: 総評と重複しない。箇条書きで1項目1行、改行で区切る（最大6項目）
  - 記述問題: 正誤の理由、不足点、別解の注意など
  - 選択肢問題: 正答の根拠に加え、主要な誤選肢がなぜ不適切かも簡潔に含める
  - 模範解答の全文は explanation に書かない（別欄で表示する）
- feedback に書いた総評を explanation で繰り返さない

生徒への呼びかけ:
- 「生徒の」「生徒は」など第三者表現は使わない（生徒にそのまま返却する前提）。
- 講評・解説では常に「あなたの」「あなたは」を使う。生徒名は返却プリントの冒頭に記載するため、本文中で名前を呼ばない。
"""


def student_address_block(student_name: str | None) -> str:
    name = (student_name or "").strip()
    if name:
        return (
            f"\n生徒名: {name}\n"
            "（返却プリントのヘッダーに記載されます。講評・解説の本文では名前を使わず「あなたの」「あなたは」と呼ぶこと。"
            "「生徒の」「生徒は」は使わない。）"
        )
    return (
        "\n生徒名: 未指定\n"
        "講評・解説では「あなたの」「あなたは」を使うこと。「生徒の」「生徒は」は使わない。"
    )
